services:
  # Backend service
  - type: web
    name: ai-portfolio-backend
    env: python
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false # Set this to your Google AI Studio key from aistudio.google.com/app/apikey
      - key: PYTHON_VERSION
        value: 3.11.0

  # Frontend service
  - type: web
    name: anshuman-portfolio-frontend
    env: static
    rootDir: frontend
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false # Set this to your backend service's public URL (e.g., https://ai-portfolio-backend.onrender.com)
